Processing:
compareImages:
contains a viewer app for seeing and understanding the comparison of block images with the shape search tool 'verse tile'

saveImageBinariesAsJson:
produces a json file which is used in the angularapp 'new.json' this is later reformatted to become 'fshape.json' with a script called 're_write_shapes_json.php' which adds catalogue information